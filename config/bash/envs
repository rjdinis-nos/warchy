# Warchy Bin PATH
PATH="$WARCHY_PATH/bin:$PATH"
PATH="$WARCHY_PATH/bin/apps:$PATH"
PATH="$WARCHY_PATH/bin/utils:$PATH"
PATH="$WARCHY_PATH/bin/install:$PATH"

# WSL
export LIBVA_DRIVER_NAME=d3d12

# Editor used by CLI
export EDITOR=nvim
export SUDO_EDITOR="$EDITOR"

# GPG Settings
export GPG_TTY=$(tty)
export GNUPGHOME="$HOME/.ssh/gnupg"

# SSH Agent (systemd socket activation)
export SSH_AUTH_SOCK="${XDG_RUNTIME_DIR}/ssh-agent.socket"

# App Settings
export BAT_THEME=ansi
export FZF_DEFAULT_OPTS='--height 30 --reverse --prompt "Select> " --ansi'
export WGETRC="$XDG_CONFIG_HOME/wget/wgetrc"

# Golang config
export GOPATH="${XDG_DATA_HOME:-$HOME/.local/share}/go"
export GOBIN="$GOPATH/bin"
export GOCACHE="${XDG_CACHE_HOME:-$HOME/.cache}/go-build"
export PATH="$PATH:$GOBIN"

# Opencode PATH
export PATH="$HOME/.opencode/bin:$PATH"

# Goggle Vertex AI
export GOOGLE_APPLICATION_CREDENTIALS=/home/rjdinis/.config/gcloud/application_default_credentials.json
